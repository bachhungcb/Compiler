Program TestCustom;
Var x : Integer;
    n : Integer;
Begin
    n := 1;
    
    (* Dùng cú pháp mới ::= kết hợp If-Return *)
    x ::= If n > 5 Return 100 Else Return 0;
    
    Call WriteI(x);
End.