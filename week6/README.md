Run instructions

1) Open a terminal in the src folder:


2) Build (Windows with GCC / Linux / macOS):

```powershell
make
```

3) Run the program:

```powershell
./kplc path_to_test_file
```

4) Clean build artifacts:

```powershell
make clean
```